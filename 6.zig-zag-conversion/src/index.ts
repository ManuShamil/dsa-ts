interface ITextProcessor {
    generateText( str: string, numRows: number ) : string
}

class TextProcess implements ITextProcessor {

    generateText(str: string, numRows: number ): string {
        
    }
}

console.log( calculateNumArrays( 7, 3 ) )